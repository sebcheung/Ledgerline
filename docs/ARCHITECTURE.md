# Architecture

See `SPEC.md` for the full build specification. This document is a short orientation to the layering, updated as phases land.

## Layers (current: Phase 7)

- `ledger/models/` — SQLAlchemy 2.0 async ORM models, one module per table group. `ledger/models/__init__.py` imports every model class so `Base.metadata` is complete wherever it's needed (Alembic, tests). Indexes and partial-unique constraints are declared in `__table_args__` so they agree with what the migrations actually create — `alembic check` runs in CI specifically to catch the two from drifting.
- `ledger/db/` — engine (`engine.py`), session factory (`session.py`), and `errors.py` (identifying which DB constraint fired inside a wrapped `IntegrityError`). `get_session()` is the FastAPI dependency; it never commits — the caller (a route handler or service function) owns the transaction boundary.
- `ledger/core/` — the ledger's domain logic, framework-free (never imports `fastapi` or anything under `ledger/api`):
  - `money.py` — the `Money` value type (parse/format at the edges; posting itself works in plain `int` minor units).
  - `invariants.py` — `signed_delta` (the one place the debit/credit sign rule is expressed), `assert_transaction_balanced`, and the read-only verifiers `verify_account_derivability` / `verify_all_derivability` / `verify_global_balance`.
  - `posting.py` — `post_transaction` and `reverse_transaction`.
  - `idempotency.py` (Phase 3) — the SPEC.md §6 protocol: `canonical_hash` (fingerprinting), `claim_key`/`complete_key`/`release_key`/`load_key` (the claim/completion/release lifecycle). The one function in `ledger.core` that commits internally — see "Idempotency" below. Phase 7 adds `sweep_idempotency_keys`, the retention `DELETE` `ledger/admin/sweep.py` runs.
  - `apikeys.py` (Phase 7) — `hash_api_key`/`generate_api_key`, and the DB-touching `create_api_key`/`lookup_active_key`/`revoke_api_key`/`list_api_keys` the admin CLI and auth dependency both build on.
  - `errors.py` — the `LedgerError` hierarchy; every raiseable failure mode is a subclass carrying an RFC 7807 `error_type`/`title`/`status`, and (Phase 3) an optional `headers` mapping for cases like `Retry-After`. Phase 7 adds `Unauthenticated` (401) and `RateLimited` (429, computed `Retry-After`).
- `ledger/schemas/` — Pydantic v2 request/response models. Validate field shape only (currency pattern, `amount > 0`, entry-count bounds); cross-entry semantics (balance, currency match, sufficient funds) are deliberately left to `ledger.core` so failures surface with the correct, stable type URI rather than a generic validation error.
- `ledger/webhooks/` —
  - `outbox.py` — `emit_event`, called from inside `post_transaction`/`reverse_transaction` in the same DB transaction as the ledger write.
  - `signing.py` (Phase 5) — `sign`/`signature_header`/`verify` (HMAC-SHA256 over `f"{timestamp}." + raw_body`, computed on bytes, never a Python f-string over the whole thing) and `build_envelope`/`serialize_envelope` (the byte-stable JSON wire shape signed and sent).
  - `dispatcher.py` (Phase 5) — `Dispatcher`: `fan_out` (outbox events → one `webhook_deliveries` row per active endpoint), `claim_batch` (`FOR UPDATE SKIP LOCKED`), `deliver` (bounded-concurrency HTTP POST + status/exception → outcome mapping), `sweep_stale_claims` (crash recovery), composed by `run_once`/`run_forever`. See "Webhook delivery" below.
- `ledger/observability/` — structured JSON logging (`structlog`) and a request-ID middleware that binds `request_id` into log context for the lifetime of each request. `metrics.py` (Phase 7) — the Prometheus `CollectorRegistry` and every SPEC.md §9 metric; see "Auth, rate limiting, and metrics" below.
- `ledger/api/` —
  - `deps.py` — `SessionDep`, `get_idempotency_key` (reads and length-bounds the `Idempotency-Key` header; optional), and (Phase 7) `V1_DEPENDENCIES` — attached to every `/v1` router.
  - `auth.py` (Phase 7) — `require_api_key` (the Bearer auth dependency) and `ApiKeyCache` (its TTL cache). See "Auth, rate limiting, and metrics" below.
  - `ratelimit.py` (Phase 7) — `RateLimiter` (the token bucket) and `enforce_rate_limit` (its dependency).
  - `openapi.py` (Phase 7) — `TAGS_METADATA`, `PROBLEM_RESPONSES`, `APP_DESCRIPTION` — OpenAPI metadata only, no runtime behavior.
  - `idempotent.py` (Phase 3) — `get_idempotent_request` (the FastAPI dependency: builds the endpoint scope string and, if a key is present, the request fingerprint — no I/O) and `IdempotentRequest.run(execute_fn)` (called from inside each idempotent route; owns the claim/execute/complete-or-release sequence). See "Idempotency" below.
  - `errors.py` — RFC 7807 `application/problem+json` rendering; registers handlers for `LedgerError`, `RequestValidationError`, `StarletteHTTPException`, `IdempotentReplay` (Phase 3), and the unhandled-exception catch-all. Phase 7 increments `idempotency_replays_total`/`idempotency_conflicts_total` from two of these handlers — see "Auth, rate limiting, and metrics" below.
  - `routes/accounts.py`, `routes/transactions.py`, `routes/admin.py` — the Phase 2 API surface (see below).
  - `routes/settlements.py`, `routes/reconciliation.py` (Phase 4) — see "Reconciliation" below.
  - `routes/webhooks.py` (Phase 5) — `POST`/`GET /webhooks/endpoints`, `GET /webhooks/deliveries`, `POST /webhooks/deliveries/{id}/retry`. See "Webhook delivery" below.
  - `routes/metrics.py` (Phase 7) — `GET /metrics`, unauthenticated by construction (no `V1_DEPENDENCIES`).
  - `health.py` — `/healthz` (liveness) and `/readyz` (DB reachability + Alembic-head check).
- `ledger/admin/` (Phase 7, not in SPEC.md §11's tree) — CLIs that need the shipped package layout to run on a deployed machine (`fly ssh console`), unlike `scripts/`: `keys.py` (`mint`/`list`/`revoke` API keys) and `sweep.py` (the idempotency retention sweep).
- `ledger/reconciliation/` (Phase 4) — framework-free, like `ledger.core`:
  - `ingest.py` — `ingest_batch`: assigns a `batch_id`, dedups within the batch on `(external_ref, amount, currency, value_date)` (currency added beyond SPEC.md §7's literal tuple), stores each line's original payload in `raw`.
  - `matcher.py` — `match`: the three-pass algorithm over two candidate lists loaded once per run, classifying in-memory and writing `settlement_lines.matched_transaction_id` directly; produces `ProposedFinding`s but never inserts them (see "Reconciliation" below for why).
  - `resolver.py` — `resolve`: per-finding-type resolution policy (auto-resolve threshold, asset-leg/clearing-account selection, sign handling), called only with the findings-insert's `RETURNING` set. `resolve_manual_adjustment` backs the manual `post_adjustment` action, bypassing the threshold and letting `LedgerError` propagate instead of being swallowed.
  - `runner.py` (not in SPEC.md §11's tree) — `execute_run`: advisory lock, DB-clock window, `matcher.match` → findings insert → `resolver.resolve` → `verify_global_balance`, all inside one transaction.
- `ledger/readmodels/` (Phase 6, not in SPEC.md §11's tree) — framework-free query modules backing the dashboard, one per panel (`balances.py`, `transactions.py`, `reconciliation.py`, `webhooks.py`). Same rules as `ledger.core`/`ledger.reconciliation`: never import `fastapi`/`starlette`/`jinja2`, accept a session, never commit. `webhooks.py`'s queue rows carry a `seconds_until_retry` computed with `now()` inside the query, not the app clock. Kept under `ledger/` rather than `dashboard/` so it's covered by the coverage gate (see `dashboard/` below) — see `docs/DECISIONS.md`.
- `migrations/` — Alembic, async-engine-driven. `0001_initial_schema` created every table, index, native enum, and the append-only trigger on `entries`; Phase 2 and Phase 3 needed no new migration. `0002_reconciliation_indexes` (Phase 4) adds `accounts.is_clearing`, its guarding `CHECK`s, and every index `settlement_lines`/`reconciliation_findings` needed but `0001` never created (see `docs/DECISIONS.md`). `0003_webhook_fanout_and_indexes` (Phase 5) adds `outbox_events.fanned_out_at` plus its partial index, and two `webhook_deliveries` listing indexes. `0004_idempotency_retention_index` (Phase 7) adds `ix_idempotency_keys_created_at`, deferred from Phase 3 until the retention sweep that uses it had somewhere to run from.
- `worker/webhook_worker.py` (Phase 5) — the standalone process entry point: builds the `httpx.AsyncClient`, wires SIGTERM/SIGINT to a stop event, and runs `Dispatcher.run_forever`. Deliberately thin — all real logic is in `ledger.webhooks.dispatcher`, which is covered by the coverage gate; `worker/` is not.
- `dashboard/` (Phase 6, SPEC.md §11) — the operator console, mounted into the same app at `/dashboard`, outside `/v1`:
  - `views.py` — thin routes: the full page, per-panel fragment endpoints (`GET /fragments/{panel}`, also what `sse.py` renders from), the SSE stream (`GET /sse`), and the gated demo trigger (`POST /demo`). Every DB/engine access goes through a FastAPI dependency (`SessionDep`, `get_dashboard_session_factory`, `get_demo_engine`) rather than importing the production engine/session factory at module scope — see `docs/DECISIONS.md` for why touching those directly from dashboard code would be unsafe under tests.
  - `data.py` — `PANEL_LOADERS`, one context-loading function per panel, composed from `ledger.readmodels`; shared by the full-page route, the fragment routes, and `sse.py`'s snapshot so there is exactly one code path building each panel's context.
  - `sse.py` — `format_sse` (SSE wire framing), `StreamConfig`, `event_stream`: periodic-poll SSE with per-panel change detection, a fresh session opened and closed every tick, and no state shared across requests. See "Dashboard (Phase 6)" below.
  - `demo.py` — `run_demo`: seeds accounts and transactions, drifts a settlement feed, runs reconciliation, registers two deliberately failing webhook endpoints, and drains one dispatcher cycle, all inside one transaction guarded by `pg_try_advisory_xact_lock`. See "Dashboard (Phase 6)" below.
  - `format.py` — pure Jinja filters (`minor_units`, `countdown`, `relative_age`, `badge_class`); `templating.py` — the `Jinja2Templates` environment, resolved from `Path(__file__)`; `errors.py` — HTML error containment for the `/dashboard` prefix.
  - `templates/`, `static/` (including vendored `htmx.min.js`/`htmx-ext-sse.js` under `static/vendor/`, never a CDN).
  - Unlike `worker/`, `dashboard/` **is** inside the coverage gate (`[tool.coverage.run] source = ["ledger", "dashboard"]`) — it's thin enough (routes, formatters, templates) that the integration suite already exercises nearly all of it, unlike `worker/`'s unavoidable process-lifecycle plumbing.

## Posting concurrency model

Postgres default READ COMMITTED, with an explicit `SELECT ... FOR UPDATE OF account_balances` over the distinct accounts touched by a transaction, rows acquired in **ascending `account_id` order**. Postgres places the `LockRows` plan node above `Sort`, so rows are locked in `ORDER BY` order regardless of the order the caller named accounts in — this is what makes two concurrent postings structurally unable to deadlock. A reversal additionally locks the original `transactions` row first, via a separate lock family that is always acquired before any balance lock, so reversals cannot introduce a new deadlock cycle either. `tests/integration/test_concurrency.py` includes a *positive control* (`test_control_unordered_locking_does_deadlock`) that deliberately locks out of order and asserts it deadlocks, so the paired "no deadlock" assertion is falsifiable rather than trivially true.

## Where each invariant is enforced

| # | Invariant | Enforcement |
|---|---|---|
| 1 | Balance | `assert_transaction_balanced`, called pre-flush in `post_transaction` |
| 2 | Immutability | DB trigger `entries_no_update`; posting uses Core selects/inserts only, so no `Entry` ORM entity is ever in the identity map to be autoflushed |
| 3 | Derivability | Maintained by construction (entries insert + balance update happen in one DB transaction under one lock); verified on demand by `verify_account_derivability`/`verify_all_derivability` |
| 4 | Currency consistency | `post_transaction`, checked against the account row returned by the `FOR UPDATE` join |
| 5 | Sign constraint | `post_transaction`, checked against the *locked* balance |
| 6 | Reversal symmetry | By construction (the mirrored entry set is the exact negation) + the `uq_transactions_reversal_of` partial unique index |
| 7 | Global balance | Follows from 1 + 2; verified on demand by `verify_global_balance`, grouped per currency |

## Idempotency (Phase 3)

`POST /v1/transactions` and `POST /v1/transactions/{id}/reverse` both accept an optional `Idempotency-Key` header, implementing SPEC.md §6 end-to-end. Two commits are involved, deliberately:

1. **The claim.** `get_idempotent_request` (a FastAPI dependency) does no I/O — it only computes the endpoint scope string (`METHOD route-template`) and, if a key is present, the request fingerprint (`ledger.core.idempotency.canonical_hash` over `{"body", "path"}` — path params are folded in specifically so an empty-bodied route like `.../reverse` still fingerprints per-target). The claim itself — `claim_key`'s `INSERT ... ON CONFLICT DO NOTHING`, or a stale-lock reclaim — happens inside `IdempotentRequest.run`, called explicitly from the route body *after* FastAPI has already validated the request against its Pydantic model. `claim_key` commits immediately. This is the one function in `ledger.core` that does; every other function in the module obeys the caller-commits rule below. Committing here is what lets 20 concurrent duplicates observe each other's `in_progress` row and fail fast with a 409, instead of all queueing behind one Postgres speculative-insert wait for the full duration of the winner's posting.
2. **The completion.** If the claim says `EXECUTE`, `run` calls the route's `execute()` closure (the actual `post_transaction`/`reverse_transaction` call), then `complete_key` (an `UPDATE ... status='completed'`, no commit of its own), then commits. **This** is SPEC.md §6's "single commit at the end" — the ledger write and the key's completion share one transaction, so a crash between them leaves zero ledger rows and the key `in_progress`, and a later retry reclaims cleanly after the TTL (`IDEMPOTENCY_LOCK_TTL_SECONDS`, default 30).

If `execute()` raises `DuplicateTransaction` (the `transactions.idempotency_key` unique-constraint backstop firing because a stale-lock reclaim raced a still-live original), `run` rolls back and re-reads the key: completed with a matching fingerprint → replay; otherwise → 409. If `execute()` raises anything else, `run` rolls back and deletes the claim (`release_key`, fenced on the `locked_at` it wrote) rather than leaving it locked for the full TTL after a request that had no ledger effect.

A replayed response is served by raising `IdempotentReplay` from `run`, rendered by its own handler with `Idempotent-Replay: true`. The stored `response_body` is an envelope (`{"v", "body", "headers"}`, JSONB, no schema change) so a replay can carry `Location` without coupling the generic protocol to transaction-shaped responses.

## Reconciliation (Phase 4)

`POST /v1/reconciliation/runs` runs synchronously inside the request, guarded by three layers: an idempotency claim (via `ReconciliationIdempotencyDep`, using a dedicated, longer TTL — `RECON_RUN_LOCK_TTL_SECONDS`, default 300s — than every other idempotent route's 30s, because a run over a window with real volume can legitimately take longer), a `pg_try_advisory_xact_lock` acquired as the first thing inside the idempotent `execute()` closure (409 `ReconciliationRunInProgress` on contention), and — for anything that still slips through — the same `transactions.idempotency_key` unique-constraint backstop every posting has.

Inside one DB transaction: read `now()` from the DB connection (never the app clock, same reasoning as idempotency staleness) to derive `window_start`/`window_end`/`cutoff_at`; `matcher.match` classifies every transaction/settlement-line candidate in memory and writes `matched_transaction_id` directly; the classification is inserted into `reconciliation_findings` via `INSERT ... ON CONFLICT (finding_type, transaction_id, settlement_line_id) DO NOTHING RETURNING ...` against an **unconditional** `NULLS NOT DISTINCT` unique index (`uq_recon_findings_open`); `resolver.resolve` is called with, and only ever processes, that `RETURNING` set; `verify_global_balance` runs before commit and a failure raises `ReconciliationRunFailed`, rolling everything back (with a `status='failed'` audit row written separately, on its own connection, so the failure isn't erased along with the rollback).

The index being unconditional (not scoped to `resolution = 'unresolved'`) and the resolver being driven off `RETURNING` (not the matcher's full classification) are the two load-bearing details that make a re-run over an unchanged window produce zero new findings and, critically, zero duplicate ledger adjustments — see `docs/DECISIONS.md` for the double-post hazard a resolution-scoped version would have reintroduced.

Every auto-resolution posts through `post_transaction` with `source='reconciliation'` and a deterministic `recon-adjust:{finding_id}` idempotency key, wrapped in the resolver's own `session.begin_nested()` (distinct from, and not covered by, `posting.py`'s existing SAVEPOINT — see `docs/DECISIONS.md`). A degraded adjustment (no clearing/suspense account configured, `InsufficientFunds`, etc.) leaves the finding `unresolved` with a logged reason and never fails the run.

`accounts.is_clearing` (one per currency, mirroring `is_suspense`) is the resolver's "implied asset account" for `unexpected_settlement` and (via a fallback, when a transaction has no single non-clearing asset leg) `amount_mismatch` adjustments.

## Transaction boundary

Route handler calls a `ledger.core` service function → the service never commits → the handler commits explicitly. `get_session()`'s `async with` block rolls back anything left open if a handler raises before committing. `ledger.core.idempotency.claim_key` (and its counterpart `release_key`) are the sole exceptions — see "Idempotency" above.

## Error contract

Every `LedgerError` subclass declares a stable `error_type` (an RFC 7807 `type` URI, kept relative exactly as SPEC.md §9 writes it), a `title`, an HTTP `status`, and (Phase 3) an optional `headers` mapping (e.g. `DuplicateTransaction`'s `Retry-After: 1`). `ledger/api/errors.py` renders these — and framework-level failures (validation errors, generic `HTTPException`, unhandled exceptions, and Phase 3's `IdempotentReplay`) — as `application/problem+json` (`IdempotentReplay` is the one exception rendered as a plain success body instead, since a replay isn't a failure). Spec-named URIs: `/errors/unbalanced-transaction` (422), `/errors/insufficient-funds` (422), `/errors/currency-mismatch` (422), `/errors/account-not-found` (404), `/errors/idempotency-conflict` (409, `DuplicateTransaction` — both the raw unique-constraint backstop *and*, via the module-local alias `IdempotencyConflict` in `ledger.core.idempotency`, the in-flight-lock conflict SPEC.md §6 describes; see `docs/DECISIONS.md` for why these share one URI), `/errors/idempotency-key-reuse` (422, Phase 3), `/errors/already-reversed` (409). Extensions: `/errors/transaction-not-found` (404), `/errors/suspense-account-exists` (409), `/errors/invalid-transaction` / `/errors/invalid-money` / `/errors/invalid-currency` (422), `/errors/invalid-cursor` (400), `/errors/validation-error` (422), `/errors/internal` (500), plus the generic `/errors/http-<status>` fallback for other framework HTTP errors; Phase 3 adds `/errors/idempotency-key-scope-conflict` (422), `/errors/idempotency-state` (500), `/errors/invalid-request-body` (400, unreachable through the API today). Phase 4 adds `/errors/reconciliation-run-in-progress` (409, `Retry-After: 5` — a new slot, not a reuse of `DuplicateTransaction`'s), `/errors/reconciliation-run-not-found` / `/errors/reconciliation-finding-not-found` (404), `/errors/finding-already-resolved` (409), `/errors/invalid-finding-resolution` (422), `/errors/clearing-account-exists` (409), `/errors/reconciliation-run-failed` (500). Phase 5 adds `/errors/webhook-endpoint-not-found` / `/errors/webhook-delivery-not-found` (404), `/errors/delivery-not-retryable` (409). Phase 6 adds no new URIs — the dashboard is read-only HTML and renders its own failures through `dashboard/errors.py`'s containment layer instead of this catalog.

## Pagination

`GET /v1/accounts/{id}/entries` and `GET /v1/transactions` use an opaque, versioned, unsigned keyset cursor over `(created_at DESC, id DESC)` — never OFFSET. The `id` tiebreak is load-bearing: Postgres's `now()` is transaction-start time, so every entry written by one `post_transaction` call shares an identical `created_at`, and a `created_at`-only cursor would skip or duplicate rows at multi-leg transaction boundaries.

## Outbox

`transaction.posted` and `transaction.reversed` events are written to `outbox_events` inside the same DB transaction as the ledger write, starting in Phase 2 — this is what gives the outbox pattern its atomicity guarantee. A reversal emits *both* events: `transaction.posted` for the reversal transaction itself (it is a real posting) and `transaction.reversed` describing the original. Phase 4's reconciliation adjustments introduce no new event type — they post through `post_transaction` like any other transaction, so `transaction.posted` covers them for free.

## Webhook delivery (Phase 5)

Draining the outbox is two independent steps, both owned by `Dispatcher` and both run every poll cycle (`run_once`, looped by `run_forever` on `WEBHOOK_POLL_INTERVAL_SECONDS`, default 1s):

1. **Fan-out.** `fan_out` claims `outbox_events` rows with `fanned_out_at IS NULL` (`FOR UPDATE SKIP LOCKED`, ordered by `created_at`), inserts one `webhook_deliveries` row per currently-active `webhook_endpoints` row (`ON CONFLICT (event_id, endpoint_id) DO NOTHING` — the idempotency guarantee), then marks the claimed events `fanned_out_at = now()`. See `docs/DECISIONS.md` for why this needed a new column rather than a schema-free anti-join, and the consequence that an endpoint registered after an event fanned out never receives that event.
2. **Delivery.** `claim_batch` claims `pending` rows whose `next_attempt_at <= now()` in one `UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING id` (marking them `delivering`), then `deliver` POSTs each claimed row concurrently, bounded by `asyncio.Semaphore(WEBHOOK_CONCURRENCY)` (default 10). Each request carries `X-Ledgerline-Event-Id`, `X-Ledgerline-Timestamp`, and `X-Ledgerline-Signature: sha256=<hex>` (`ledger/webhooks/signing.py`).

Outcome mapping in `_deliver_one`: 2xx → `succeeded`; 429 or 5xx or a timeout/transport-level exception → `schedule_retry`; any other 4xx → `dead` immediately (a client error won't fix itself). `schedule_retry` computes a **full-jitter** backoff (`compute_backoff`: `random.uniform(0, min(base * 2**(attempt-1), cap))`, base 1s / cap 1h / `WEBHOOK_MAX_ATTEMPTS` 8) and either reschedules `pending` or, past the attempt cap, moves the row to `dead`.

A separate step, `sweep_stale_claims`, moves any row stuck `delivering` with `claimed_at` older than `WEBHOOK_STALE_CLAIM_SECONDS` (default 60) back to `pending` without touching `attempt_count` — this recovers from a worker crashing between the claim and the delivery, and is what makes delivery **at-least-once**: a receiver may see the same `X-Ledgerline-Event-Id` more than once and must dedupe on it (see `README.md`).

`POST /v1/webhooks/deliveries/{id}/retry` is the manual DLQ replay path: `dead`-only, resets `attempt_count` to 0, CAS-guarded the same way `resolve_finding` (Phase 4) is.

`worker/webhook_worker.py` is the process that runs this loop continuously in production; `tests/faults/` drives `Dispatcher`'s steps directly (never the loop) against a real `uvicorn`-hosted mock receiver (`tests/mock_receiver/`), so every SPEC.md §10 webhook fault (timeout, connection reset, 5xx/429, 400, and a crashed-worker stale claim) is deterministic without sleeping for a real delay.

## Dashboard (Phase 6)

Four live panels — account balances, recent transactions, reconciliation run history and open findings, and the webhook delivery queue with retry countdowns and DLQ depth — each backed by one `ledger.readmodels` module and rendered by one Jinja partial (`dashboard/templates/partials/_<panel>.html`) shared across three call sites: the full-page route (`GET /dashboard`), the no-SSE fragment route (`GET /dashboard/fragments/{panel}`), and the SSE stream.

`GET /dashboard/sse` is periodic-poll SSE, not Postgres LISTEN/NOTIFY: every `DASHBOARD_SSE_INTERVAL_SECONDS` (default 2s), `event_stream` opens a fresh session, re-renders all four panels, and emits an SSE event only for the panels whose rendered HTML changed since the last tick (compared by a `blake2b` digest) — consumed by htmx's `sse-swap` extension, so the payload is rendered HTML, not JSON. No session or connection is held between ticks, and no state is shared across requests; a heartbeat comment keeps the connection alive when nothing changed, and a hard `DASHBOARD_SSE_MAX_STREAM_SECONDS` cap ends the stream cleanly (the browser's `EventSource` reconnects automatically). `StreamConfig.max_events` bounds the number of ticks and is supplied by an overridable FastAPI dependency (`get_stream_config`), which is what lets tests drain the generator to completion instead of abandoning a live one. `dashboard/static/ledgerline.js` ticks retry countdowns once a second between SSE refreshes, using only elapsed client-side time since the value was rendered — it never compares the browser's clock against the server's, so a wrong workstation clock cannot produce a wrong countdown.

The **demo scenario** (`dashboard/demo.py::run_demo`, triggered by `POST /dashboard/demo` — gated by `DEMO_ENABLED`, default `false`, 404 when disabled) seeds demo accounts (idempotently — looked up before creation), posts ~20 transactions, backdates most of them past the reconciliation cutoff (so drift is reportable instead of suppressed as `in_flight`), drifts a settlement feed (`ledger.reconciliation.feed.generate_feed`, moved here from `scripts/gen_feed.py` in this phase) with a perturbation range that straddles the auto-resolve threshold (so a run yields both `auto_resolved` and `unresolved` findings), registers two endpoints pointed at deliberately failing URLs, runs reconciliation, and drains one dispatcher cycle — all inside **one** DB transaction, single commit, guarded by `pg_try_advisory_xact_lock` (a session-per-commit design would release that lock at the first intermediate commit; see `docs/DECISIONS.md`). Re-runnable but deliberately not idempotent: each call appends a fresh scenario rather than being a no-op. `scripts/demo.py` is a thin CLI over the same function, for parity with `worker/webhook_worker.py`'s relationship to `ledger.webhooks.dispatcher`.

HTML error containment (`dashboard/errors.py`) sits in front of, not instead of, the RFC 7807 handlers below: it delegates to the exact same handler for any request outside `/dashboard`, and renders an HTML page (or, for `HX-Request`, a 200 toast fragment, since htmx never swaps a non-2xx response) for anything under it.

## Auth, rate limiting, and metrics (Phase 7)

Every `/v1` router carries `V1_DEPENDENCIES = [Depends(require_api_key), Depends(enforce_rate_limit)]` (`ledger/api/deps.py`) via `include_router(..., dependencies=V1_DEPENDENCIES)`, in `ledger/api/main.py`. `/healthz`, `/readyz`, `/metrics`, and every `/dashboard/*` route are unauthenticated by construction — none of their routers are given this list — rather than by an allowlist; `tests/unit/test_openapi.py` enforces the split as a contract test.

**Auth.** `require_api_key` (`ledger/api/auth.py`) reads `Authorization: Bearer <key>`, hashes it (`ledger.core.apikeys.hash_api_key`, SHA-256), and looks it up against `api_keys.key_hash` (the table Phase 1 modeled and left unused until now). The lookup is cached per app instance (`ApiKeyCache`, TTL `API_KEY_CACHE_TTL_SECONDS`, default 30s, with negative caching) — never a module-level global, so every test's `create_app()` starts with an empty cache. Every failure mode (missing header, wrong scheme, unknown hash, inactive key) raises the same `Unauthenticated` (401) — no distinction, so a response can never confirm a guessed key was ever valid. `ledger/admin/keys.py` (`mint`/`list`/`revoke`) is the CLI; it lives under `ledger.admin`, not `scripts/`, so it runs on a deployed machine (`scripts/` ships in neither the wheel nor the Docker image).

**Rate limiting.** `RateLimiter` (`ledger/api/ratelimit.py`) is a per-key token bucket (100 req/s, burst 200 — `RATE_LIMIT_RPS`/`RATE_LIMIT_BURST`), refilled lazily on read, one instance per app (`app.state.rate_limiter`). In-process, per SPEC.md §9 — the consequence is that the effective limit is per uvicorn worker process, which is why `fly.toml` pins exactly one worker per web machine. Exhaustion raises `RateLimited` (429), which computes an exact `Retry-After` via the `problem_headers()` hook `LedgerError` reserved for it back in Phase 2.

**Metrics.** `GET /metrics` (`ledger/api/routes/metrics.py`, unauthenticated, gated by `METRICS_ENABLED`) serves `ledger.observability.metrics.REGISTRY` in Prometheus text format. `transactions_posted_total`/`entries_written_total`/`posting_latency_seconds` are incremented inline in `ledger/core/posting.py`; `idempotency_replays_total`/`idempotency_conflicts_total` are incremented in `ledger/api/errors.py`'s two relevant handlers, not at `ledger.core.idempotency`'s own log sites (a `DuplicateTransaction` is sometimes converted into a replay — see `docs/DECISIONS.md`). `webhook_deliveries_total`, `webhook_dlq_depth`, and `reconciliation_findings_total` are `Gauge`s refreshed from their tables by `refresh_db_gauges` at scrape time, since the processes that produce those rows (`worker/webhook_worker.py`; a synchronous reconciliation run) have no HTTP server of their own to scrape.

**Idempotency key retention.** `ledger.core.idempotency.sweep_idempotency_keys` deletes `completed` rows older than `IDEMPOTENCY_RETENTION_DAYS` (default 30), backed by `ix_idempotency_keys_created_at` (migration `0004`). `ledger/admin/sweep.py` is the CLI; a Fly scheduled machine runs it (`docs/DEPLOY.md`), not a background loop in the API or worker process.

## Deploy (Phase 7)

`fly.toml` defines two process groups sharing one image: `app` (`uvicorn ... --workers 1`, exactly one — see above) and `worker` (`python -m worker.webhook_worker`, otherwise never running in production). `[deploy].release_command = "alembic upgrade head"` runs in a temporary machine with the app's secrets, before any traffic shifts, and aborts the deploy on failure; `/readyz`'s existing Alembic-head check is the second, continuous layer. `Settings._normalize_database_url` (`ledger/config.py`) rewrites a libpq-form `DATABASE_URL` (what `fly postgres attach` sets) to the `+asyncpg` driver both `ledger/db/engine.py` and `migrations/env.py` require.

`.github/workflows/cd.yml` deploys on `workflow_run` from CI, gated on `conclusion == 'success'` and a push to `main`, pinned to the validated commit's SHA; every deploy step is additionally gated on `FLY_API_TOKEN` existing, so the workflow lands as a verified no-op — see `docs/DEPLOY.md` for the one-time manual setup (`fly launch`, Postgres provisioning, the first API key, the scheduled sweep machine, and the CD secret) this phase deliberately does not perform.

## Not yet implemented

`worker/recon_scheduler.py` is the one remaining empty package stub — left stubbed deliberately; see `docs/DECISIONS.md` Phase 7 for why. With Phase 7 landed, every item in `SPEC.md` §12's build order is otherwise complete.

## Known, accepted gap (Phase 4)

A transaction reversed *after* its settlement line already matched leaves a settled line against a now-net-zero ledger effect; no `SPEC.md` §7 finding type detects this. Not fixed — see `docs/DECISIONS.md` Phase 4 for why.
